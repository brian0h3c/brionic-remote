# Build output
/brionic-remote
/brionic-remote.exe
/dist/

# Frontend build output (regenerated by `npm run build`); keep the embed dir
/web/dist/*
!/web/dist/.gitkeep
/web/node_modules/

# Vault files (never commit secrets)
*.vault
*.vault.tmp
*.vault.files/

# OS / editor cruft
.DS_Store
*.log
